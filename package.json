{
  "name": "vortex-shell-workspace",
  "private": true,
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  },
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "tauri:dev": "tauri dev --no-watch",
    "tauri:build": "tauri build"
  }
}
